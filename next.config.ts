import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['warpcast.com'], // Add the domain here
  },
  // Other configuration options can remain here
};

export default nextConfig;
